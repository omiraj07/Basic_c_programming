#include<stdio.h>
int main(){
    int no;
    float deci;
    char name;
    double digit;
printf("the size of integer is \n %zu bytes\n",sizeof(no));
printf("the size of float is \n %zu bytes\n",sizeof(deci));
    return 0;
}